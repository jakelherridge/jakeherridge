---
layout: layouts/base.njk
title: {{ title | default("About") }}
permalink: "/about/index.html"
---

<section class="max-w-4xl mx-auto p-6">
  <h1 class="text-2xl font-bold mb-4">{{ title or "About" }}</h1>

  {# If CMS provides a markdown "body", render it; otherwise show your current copy #}
  {% if body %}
    <div class="prose prose-invert max-w-none">
      {{ body | safe }}
    </div>
  {% else %}
    <p class="text-[color:var(--muted)]">Tell your story here…</p>
  {% endif %}
</section>